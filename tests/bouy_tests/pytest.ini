[pytest]
# Isolated pytest configuration for bouy tests
addopts = -p no:cacheprovider -p no:warnings
norecursedirs = .. fixtures
python_files = test_bouy*.py
testpaths = .
# Don't look for conftest files in parent directories
confcutdir = .