# HSDS Federation — P2 Pull Ingest (bite-sized plan)

> Sibling of the living plan [`2026-06-03-hsds-federation-core.md`](2026-06-03-hsds-federation-core.md) and the design of record [`../specs/2026-06-03-hsds-federation-core-design.md`](../specs/2026-06-03-hsds-federation-core-design.md). Build slice-by-slice: TDD red-first → per-slice RED-tier PR Gauntlet → owner-override-merge on green (standing owner authorization: merge each federation slice PR once CI is green and the Gauntlet is clean).

> **REVISION 2 (2026-06-10) — deep design+spec review folded in.** Two adversarial review workflows (verdicts: design = **right_with_corrections**; spec = **not-submittable-today, document-shaped gap**) reshaped this plan. Full findings: [`../research/2026-06-10-federation-design-and-spec-review.md`](../research/2026-06-10-federation-design-and-spec-review.md) (READ IT FIRST). Owner decisions 2026-06-10: **(D1) ADOPT the W3C `eddsa-jcs-2022` cryptosuite** (replaces the `ed25519-jcs-2026` lookalike — wire change, do it FIRST while zero foreign impls exist); **(D2) fold all review corrections into this plan** (done below).

## ▶ RESUME POINTER (start here — for the next session / Opus)

**Done:** P1 verifiable-publish COMPLETE+closed (#522). P2 Slice 1 (corroboration-counter extraction, `app/reconciler/corroboration.py`) MERGED (#578). The slice table below is REVISION 2 — five new review-driven slices (**W, 5.5, 5.6, 6.5, NR**) added to the original set.

**Next action:** **Slice W (issue #579)** (adopt `eddsa-jcs-2022`) — it is a wire/crypto change to P1-shipped code (`envelope.py` proof + the proof-bearing conformance vectors), RED-tier, and MUST land before the two-node demo or any partner pins bytes. Then Slice 2 (location_creator IX extraction), then 3 → … in dependency order. All **10** review critical/high findings (3 critical + 7 high) are folded into specific slices (W, 3, 4, 5, 5.5, 5.6, 6, 6.5, 7a, 7b, 8, 12, NR) and flagged ⚠️R# below.

**How:** each slice = TDD red-first → per-slice RED-tier Gauntlet (Workflow) → PR → owner-override-merge on green CI. The build host is memory-constrained: run ONE review workflow at a time. The verify primitive stack is SHIPPED (reuse, don't rebuild): `tests/test_federation/conformance/` (runner.py `verify_level2` + `verify_consistency_to_head`; adapter.py `RefAdapter`); `identity.public_key_from_multibase`; `envelope.verify_envelope`; `activities.validate_activity`. Deterministic validator-stage entry precedent: `app/replay/replay.py:enqueue_to_validator`.

## Objective & acceptance

P2 makes a PPR node **consume** a peer's verifiable publish: pull `/export` → **verify** (peer DID key from its `/.well-known/did.json` → signed checkpoint note → every envelope signature → RFC-6962 inclusion vs the held root → consistency vs the last-pinned per-peer checkpoint → `validate_activity` verb rules → allow-list/idempotency/budget/injection) → enqueue a **scraper-shaped `LLMJob`** → ingest through the unchanged Content-Store→LLM→Validator→Reconciler pipeline as `source_type='federated_node'` → **corroborate by distinct ORIGIN**.

**🎯 Headline acceptance (the partner-integration gate):** **two SEPARATELY-RUNNING PPR nodes** — each its own deployment, DB (or schema), `did:web` DID, and DIFFERENT dataset — pull → verify → ingest → corroborate each other over **real HTTP**, **bidirectionally**. An unnamed partner integrates once we demonstrate this with two of our own nodes; the partner then joins as the real complementary-authority node. ⚠️ NO Feeding America feed exists (the old acceptance is void). A foreign/non-PPR node is P7.

**P1 already shipped the entire verify primitive stack** (reuse, do not rebuild): `tests/test_federation/conformance/runner.py` `verify_level2` + `verify_consistency_to_head` (pure dispatch over a `get(path)->Resp` callable **+ an `HsdsFxAdapter` (`RefAdapter`)** — transport-agnostic, reusable verbatim against a live URL); `identity.public_key_from_multibase`; `envelope.verify_envelope`; `activities.validate_activity` (stateless verb wire rules — it explicitly documents that ALL stateful ingest policy is P2's job).

## Owner decisions (2026-06-09)

1. **`verified_by='network'` authority tier — BUILD NOW (owner chose the new tier over auto-parity).** Override precedence becomes `auto < network < {claimed, source, admin}` (human). Semantics: a federated `network` Update **may overwrite `auto`** fields and corroborates; an `auto` scrape **may not overwrite** a `network` field; `network` **may not overwrite** any human tier (admin/source/claimed stay owner-guarded). `network` is conferred only on records ingested from an **allow-listed authoritative partner** peer (not every `federated_node` — a non-authoritative peer ingests at `auto`-parity). Confidence: override precedence is **tier-based, independent of the confidence number**; `network` records get the standard build-up score (interfaces reserved for a distinct band, but P2 keeps the cap rules and ranks by tier). This pulls part of the P5/VC-trust tier forward — the exact band + the override matrix are pinned in Slice 8 with a fixture and Hypothesis tier-ordering test. `scoring.py:HUMAN_VERIFIED_SOURCES` stays human-only; a new `OWNER_PRECEDENCE` ordering encodes `auto < network < human`.
2. **§6.6a plain-HSDS consumer — BUILD NOW as a general capability (owner chose build over defer).** Slice 14 ships the snapshot-diff machinery (Service-level deltas, N-consecutive-absence tombstones) exercised by synthetic fixtures; no live target yet (no FA feed). Stays subordinate in priority to the PPR-peer path that gates the partner demo.

## Wire-freeze decisions (RESOLVED 2026-06-10 — must land before the partner pins bytes)

These touch signed bytes or vector contents and become immutable once a P2 peer integrates. Decide+implement now while zero foreign impls exist (most ride **Slice W**):

- **D1 — Cryptosuite: ADOPT W3C `eddsa-jcs-2022` (owner-approved).** Replace the `ed25519-jcs-2026` lookalike with W3C Data Integrity: `proof.type="DataIntegrityProof"`, `cryptosuite="eddsa-jcs-2022"`, `proof.proofValue` = **multibase (base58btc `z…`)** Ed25519 over the JCS of (proof-options ∥ document) per the cryptosuite, `verificationMethod`, `created`, `proofPurpose="assertionMethod"`. **Migration is uniquely cheap now:** the Merkle leaf, checkpoints, and content-address are all **proof-independent** (leaf = `JCS(envelope ∖ {id,proof})`), so adopting DI changes only the `proof` object + re-bakes the `envelope_proof` conformance vector — NOT the leaf/inclusion/consistency/checkpoint vectors. Decide the proof-options canonicalization detail against the W3C eddsa-jcs-2022 test vectors (vendor them). Verifier resolves the key from the signed `actor`/`origin` (NOT `verificationMethod`) — see ⚠️REVIEW finding 9.
- **D2 — @context + neutral profile-URI domain:** `@context` = the versioned HSDS-FX profile URI on a neutral (non-PPR) domain; HSDS data version conveyed via the discovery doc `hsds_versions` set-membership (§8.4), never an unminted openreferral URI. Resolves the 3-way @context inconsistency. Advertise **3.1.1** (honest per Principle II).
- **D3 — envelope vocabulary:** KEEP ActivityStreams names on the wire (`actor`/`attributedTo`/`origin`, `Update`/`Announce`/`Delete`) + write the normative crosswalk to publisher/steward/source + `org-id.guide` (spec Appendix D; HSDS issues #558/#553).
- **D4 — `license`:** define normatively as an SPDX id or absolute URI (not free text in a SIGNED field); regenerate vectors with a **neutral example** (e.g. CC-BY-4.0); license the corpus itself (CC0/CC-BY). Strip the bespoke `sandia-ftgg-nc-os-1.0` from canonical vectors.
- **D5 — conformance classes:** split into **HSDS-FX Core (proof OPTIONAL — the Tier-0/1 no-crypto on-ramp)** + a **Verifiable** tier. Makes the "no crypto to participate" pitch conformant.
- **D6 — Announce shape (⚠️REVIEW finding 8):** an Announce MUST embed the origin's signed envelope (ideal) or at minimum its content-address `id`, so corroboration is cryptographically grounded, not unsigned hearsay.
- **D7 — PII redaction vs append-only (resolves §11.8 vs §6.2g):** leaf-payload redaction with **leaf-hash retention** — destroy the archived preimage bytes on a redaction event but keep the leaf hash, so root@N + all proofs stay valid.
- **Auto (no owner call):** verb registry (closed set {Update,Announce,Delete}, unknown → reject); I-JSON integer domain ±(2^53−1) cite RFC 7493; rename `X-Federation-*` headers → unprefixed `Federation-*` (RFC 6648); `verify_note` accept-set aligned to C2SP signed-note.

## Principle-IX gate (Task 0 — FIRST, blocks the §12 corrections)

Both edited files exceed 600 lines and neither is in the constitution §IX Known-Violations table (so the table changes either way). **Decision: HYBRID EXTRACTION** (the proven P1 `location_commit.py` precedent — "extract along responsibility boundaries", constitution.md:207), co-locating each extraction with where its P2 edit lands:
- **`merge_strategy.py` (888)** → extract the corroboration-counting responsibility (`merge_location` source query + distinct-source count + `apply_source_corroboration`, ~:207–326) into `app/reconciler/corroboration.py`. The §12.1 origin-dedup change (Slice 7) lands here.
- **`location_creator.py` (968)** → extract `find_matching_location_with_lock` (~:141–315) and/or `create_location_source` (~:434–542) into `app/reconciler/location_matching.py` / `location_source.py`. ⚠️ NB: `app/reconciler/location_match.py` **already exists** (the P1 `LocationPreprocessor` extracted from location_commit.py) — do NOT collide with it; pick a fresh module name. The `federated_node` ON CONFLICT (Slice 4) + the exact-`federation_id` Tier-0 lookup (Slice 11) land here.

## Live multi-node demo design (the headline — corrected per critique)

The `federation_log` is one table per DB and the read router binds a single process-global config + cached engine (`router.py:38-48`). Genuinely-separate nodes need (a) **one piece of new production code**: a minimal per-app config/session **DI seam** on the router — `app.state.federation_settings` (DID / signing key / DB) + a session factory passed at register-time, falling back to the global singleton (~20 lines; router.py is <600, no IX gate) — **Slice 15**; and (b) DB isolation.

⚠️ **Critique correction:** `TEST_DB_SCHEMA` (conftest.py:138) is **dead config** — nothing consumes it. The harness must **BUILD** schema isolation, not reuse it (feasible — the test DB user is superuser: `CREATE SCHEMA node_a/node_b` + a session factory issuing `SET search_path`, because all federation/location tables are referenced UNQUALIFIED — with ONE exception to handle in Slice 15/16: `publish.py:161` probes `to_regclass('public.dedup_run_audit')` schema-pinned).

- **Pytest harness (CI acceptance, Slice 16, RED):** two FastAPI apps, each its own identity (`_SEED_A`/`_SEED_B` pattern) + its own schema; **httpx `ASGITransport`** between them (in-process, no socket, no SSRF IP to block); run the REAL discover→verify_level2→ingest(as `federated_node`)→corroborate loop, then bidirectional A↔B; assert origin-dedup, lone-peer gating, the `network`-tier merge.
- **Dev two-node (Slice 17):** node-B as **extra services in the ONE compose project** (bouy hardcodes `COMPOSE_PROJECT_NAME`, `bouy:46` — a second project can't go through bouy), via a `plugins/ppr-federation-demo/` overlay (auto-discovered; doubles as the §8.6 clone-able partner-onboarding artifact). Needs a **`FEDERATION_FETCH_ALLOW_HOSTS` SSRF dev-allowlist seam** (fetch.py blocks internal IPs, so a same-host peer is unreachable without it) — operator decision deferred to Slice 17.

## Slices (dependency-ordered — REVISION 2)

`⚠️R#` = folds in a deep-review finding (see the review doc). `RED` = RED-tier Gauntlet mandatory.

| # | Slice | RED? | Dep |
|---|-------|------|-----|
| 1 | ✅ **DONE (#578)** — extract corroboration counter → `app/reconciler/corroboration.py` (+ org/service mixin; merge_strategy 879→437) | | — |
| **W** | **⚠️ Wire-freeze: adopt W3C `eddsa-jcs-2022` (D1).** Rewrite `envelope.py` proof (DataIntegrityProof + cryptosuite + multibase proofValue + created/proofPurpose); vendor the W3C eddsa-jcs-2022 KAT; re-bake the `envelope_proof` **+ `envelope_assembly`** conformance vectors (BOTH embed the proof object; leaf/inclusion/consistency/checkpoint vectors UNCHANGED — proof-independent); apply the auto wire-freeze items (header rename, I-JSON note, verb registry, verify_note accept-set). **Do FIRST — wire/crypto change to shipped P1 code.** Issue #579. | RED | — |
| 2 | **Task 0b (IX gate)** — extract matching ladder / `create_location_source` from `location_creator.py` (968) → `location_match.py`/`location_source.py` (behavior-preserving, like Slice 1) | | — |
| 3 | **Models/migration** — `federation_peer` (DID, actor URL, pinned signing+recovery key fingerprints, `trust_tier`, policy, `enabled`, **the peer's last raw SIGNED checkpoint note bytes** ⚠️R10, cached size/root) + `federation_peer_cursor` (shared `(actor,sequence)` idempotency, pull cursor, reserved push high-water; `CursorStore` dual-backend protocol). **`location_source` columns** ⚠️R4/R11: `federation_id` (+index, m9), `origin_did`, `delivered_by_did`, `verb`, `asserted_at`, `origin_sequence`. | | — |
| 4 | `federated_node` partial unique index + ON CONFLICT branch in `create_location_source`; stamp the envelope content-address `id` + origin DID on the row (⚠️R1/R11 ties every DB row to signed wire bytes) | | 2,3 |
| 5 | **⚠️R1 (CRITICAL) — INVERTED ingest path.** `app/federation/enqueue.py`: verified PPR-peer envelopes (already JCS-valid HSDS) take a **DETERMINISTIC no-LLM structured path** — validate against the HSDS Pydantic models → enqueue at the **validator stage** (precedent: `app/replay/replay.py:enqueue_to_validator`) → reconciler, byte-faithful. LLM aligner reserved for genuinely free-form §6.6a sources only. VALIDATOR_ENABLED routing (federated still scored when off — Principle VI). | RED | 4 |
| 5.5 | **⚠️R2 (CRITICAL) — SCALE / #562 (BLOCKING before 16/17).** Tier A: request-scoped subtree-root memoization in `read_export`/`read_history` + short-TTL `/checkpoint` cache + `/export` limit clamp + a perf-budget test. Bit-identical output, KAT-gated. (Measured: page 9s@5k, full pull O(N²) 43s@5k; AWS 30s timeout @~16k; prod 87.7k locations.) Tier B (persisted Merkle node table, O(log N)) = the hard gate on prod-publish enablement, scheduled post-demo (RED-tier). | RED | — |
| 5.6 | **⚠️R3 (CRITICAL) — prune safety.** Archive-format fix: store the FULL reconstructed envelope (preimage + id + proof, format-versioned) not bare preimage (also closes §6.2g "still origin-verifiable from archive"); backfill any pruned leaves. `prune_to_horizon` refuses/alarms when the read path has no persisted-tree/archive-capable serving (mirrors the bouy refuse-without-archive guard). | RED | 5.5 |
| 6 | **Verify-before-enqueue pull consumer `app/federation/ingest.py`** (§6.6 order; reuse `verify_level2` + `verify_consistency_to_head` **with `RefAdapter`** + `public_key_from_multibase`; cursor stores held size/root + **the raw signed note** ⚠️R10). **⚠️R9:** add verifier checks — resolve key from signed `actor`/`origin` NEVER `verificationMethod`; require `proof.type` ∈ supported suites; bind verificationMethod DID == actor; **pull-side pinned-key enforcement** (did.json key must match the pinned fingerprint → else `federation_peer_key_mismatch` alarm). **Lands the P0-deferred `fetch.py` DNS-rebinding connect-pin + streaming byte-cap** (first real outbound fetch). | RED | 5,3 |
| 6.5 | **⚠️R6 — wire-shape hardening (before the demo + extraction).** Extend the signed §8.2 aggregate to include structured **addresses, phones, languages, accessibility, services-at-location** (additive within HSDS 3.1.1). Cold-start parity guard re-run as the gate. The substrate must protect data a consumer can actually use. | RED | W |
| 7a | **⚠️R8 — origin-deduped corroboration (§12.1).** Widen the corroboration query to include `federated_node`; count distinct **ORIGIN DIDs** (relayer in `delivered_by_did`); pin `scraper_id='federation:<ORIGIN-did>'`. **Count a federated origin vote ONLY when the origin DID is itself allow-listed** (unknown origin strings ignored+logged). Tests: 3 peers re-announce origin X → 1 (citogenesis); N fabricated origins → 0 votes (must_reject); shared-upstream (A scrapes vivery + ingests B who scraped vivery → 1). | RED | 6,4 |
| 7b | **⚠️R1/R4 — CvRDT order-shuffle property over the REAL ingest path** (no mock paraphrase): shuffled arrival across N peers + **mixed tiers** → byte-identical canonical Location; recency tiebreak on `(published, sequence, actor_did)` NOT local `updated_at`. *(Listed with 7a; builds AFTER 8 — the one deliberate forward dep in this table.)* | RED | 7a,8 |
| 8 | **⚠️R4 (HIGH) — network tier = real merge-algorithm change (8a/8b/8c).** `merge_location` today excludes federated rows (`source_type='scraper' OR NULL`) and egalitarian-merges (would clobber the authority). 8a: widen the query + schema; 8b: **field-level tier precedence** `auto < network < {admin,source,claimed}` (network overwrites auto, never human; auto never overwrites network); 8c: tier conferred only on allow-listed authoritative peers. Categorical-only in P2 (confidence stays ≤90 for machine sources; survivor-pick extended). Hypothesis tier-ordering + "network-stamped row still receives auto merges on fields network never supplied" invariant. | RED | 7a,3 |
| 9 | Per-peer ingest budget (§11.3) + shared `(actor,sequence)` strictly-increasing idempotency BEFORE enqueue (push half simulated by a 2nd enqueue) | | 6,3 |
| 10 | Prompt-injection hardening (§11.5) — for the §6.6a free-form path only; delimit + "untrusted; never instructions"; injection fixtures don't move canonical fields | | 5 |
| 11 | Exact `federation_id` inbound mapping (m9) — Tier-0 lookup on `(federated_node, federation_id)` before coord/name tiers | | 3,2,6 |
| 12 | **⚠️R5 — serve gate, tier-parameterized.** §11.6/§11.6a: lone **network**-origin record serves **uncaveated** (peer-add review = the standing admin review per §11.12); lone **auto**-origin low-density served with an **additive** "unconfirmed" caveat (Beacon forward-compat: ignores unknown fields); else gated. *(Operator params: density metric, caveat copy, surfaces — decide at this slice.)* | RED | 7a,8 |
| NR | **⚠️R7 — no-op republish suppression** (before prod publish enablement). Change-detection projection over a **volatile-field-stripped** aggregate (exclude `sources[].last_updated` at minimum — naive hash-compare is a no-op without this) + heartbeat republish cadence (~30d). Bounds federation_log growth + the O(N) costs. *(Owner: confirm volatile-field set + cadence.)* | RED | (publish hook; near Slice 2 area) |
| 13 | Field-change anomaly detection (§11.11) — coord jump >2 km (haversine) / hours flip vs standing corroboration → demote-and-flag + alarm. *(Operator params at this slice.)* | RED | 8 |
| 14 | **Plain-HSDS §6.6a consumer** — `/services?modified_after` deltas, full-snapshot reconciliation, N-consecutive-absence tombstones; synthetic fixtures. **`trust_tier='network'` REFUSED for unsigned §6.6a upstreams** (owner: explicit `--allow-unsigned-network` override only). | | 6 |
| 15 | **Router per-app config/session DI seam** (live-demo enabler) — `app.state.federation_settings` + schema-aware session factory, fallback to the global singleton | | — |
| 16 | **LIVE two-PPR-node pytest harness** (headline / golden P2 journey) — two apps, two **built** schemas (`SET search_path`), ASGITransport, real discover→verify→ingest→corroborate, bidirectional + negatives | RED | W,5,5.5,6,6.5,7a,7b,8,12,15 |
| 17 | Pull-consumer loop dual-env (bouy worker / EventBridge Lambda → ingest SQS) + dev two-node `plugins/ppr-federation-demo/` overlay (node-B = extra services in ONE compose project) + `FEDERATION_FETCH_ALLOW_HOSTS` SSRF dev seam + **onboarding micro-kit** (`python -m app.federation keygen`, a peer-add script, a one-page partner runbook — ⚠️R13) | | 16 |
| 18 | P2 AWS observability (Principle XIV) — ingest SQS depth + DLQ alarms, pull-consumer Lambda Error/Throttle, budget-rejection alarm, key-mismatch + anomaly alarms, dashboard widgets, `infra/tests/` | | 17 |
| 19 | **Docs + design-doc scrub** — CLAUDE.md federation P2 section; **scrub void FA-feed refs** from design §1.1/§6.6a/§17/§22 + INTEROP_PENDING confirmers (issue #541 + epic doc scrubbed 2026-06-10); **fix design §6.2a Merkle-leaf definition** (leaf = `JCS(envelope∖{id,proof})` under 0x00 prefix, not the content-address); fix §8.1 worked example (add `license`, correct @context, explicit per-verb field table); full `./bouy test` gate | | 18 |

**Next slice: W** (eddsa-jcs-2022). Then 2, 3, 15 are dependency-free and can interleave. The 5.5/6.5 scale+shape slices gate the live demo (16); **5.6 gates the next prod retention prune** (the demo never prunes, so it's not a 16 dep — but it MUST land before any further prod `prune_to_horizon` run).

**Post-P2 hard gate (not in P2, but tracked):** Tier B persisted Merkle node table (§5.5) before prod-publish enablement AND the first prod retention prune — RED-tier, eliminates the OOM + post-prune S3 amplification.

## P2/P3 boundary (do NOT leak P3)

- **Pull-only.** `/inbox` push + the outbound signed sender (`app/federation/outbound.py`) are **P3**. The live demo PULLS both directions; it does not push.
- The `(actor,sequence)` idempotency KEY is P2 (Slice 9); the cross-transport pull+push integration test and the inbox verify/guard chain are P3.
- Reconciler §12.3 corrections (origin-dedup, ON CONFLICT, owner-guard, anomaly, CvRDT) are transport-agnostic — they serve P3 push unchanged.
- Peer-remove recovery (§11.4), mass-anomaly inbox alarms, the `./bouy federation` peer-management CLI family (P4), witness mesh / Announce relay / outbound emission (P6), §12.2 provenance-weighted corroboration (P6), full recovery-key verify-side enforcement (P3+/P7), foreign-impl interop (P7) — all out of P2.

## Remaining operator decisions (surfaced at their slice, not now)

- **Slice 8:** the exact `network` confidence band (vs human 91–100 / auto ≤90) and the full override matrix edge cases.
- **Slice 12:** equity-floor density metric + the "unconfirmed" caveat string + which surfaces carry it (/export, PTF, Beacon, all).
- **Slice 13:** anomaly demote magnitude + `is_canonical=FALSE` vs flag-while-served + federated-vs-federated only or also auto-vs-federated.
- **Slice 17:** is `FEDERATION_FETCH_ALLOW_HOSTS` an acceptable production surface or strictly dev-gated.
- **Slice NR:** the volatile-field exclusion set (beyond `sources[].last_updated`) + the heartbeat republish cadence (~30d default).
- **Slice W/6.5 (review R12 — wire-touching, decide before a peer pins bytes):** keep the export-time `inclusion_proof` injection at envelope top level, or nest it under a wrapper key.

## GitHub tracking (the issue side of the workflow — keep in lockstep)

**Ledgers.** Epic **#519** is the cross-phase ledger — post a status comment at each phase transition or major plan revision. **#523** is the P2 phase ledger — when the plan materially revises, post a status comment AND banner-correct any superseded body text in place (never silently rewrite; the comment trail is the audit). #523 is closed by the phase-closing slice (19) with a completion summary. Both got REVISION 2 status comments 2026-06-10.

**Per-slice issues.** Create a slice's issue when the slice STARTS (not batch up front — slices reshape as reviews land): title `P2 Slice <n> — <short name>`, body = what/why + plan/design §-refs + acceptance; label like prior federation slices. The slice PR description says `Closes #<slice-issue>` and references #523. When the PR merges: confirm the slice issue auto-closed, tick/annotate the slice row here (✅ + PR#), and comment on #523 if the merge changes anything downstream. Slice W's issue is **#579**.

**Cross-cutting issues — referenced, not closed by P2 slices:**
- **#562** (scale): Slice 5.5 = Tier A, its PR *references* #562; the issue stays open until post-P2 Tier B (persisted Merkle node table) ships. Status comment posted 2026-06-10 with the measured numbers.
- **#540** (HSDS-FX spec artifact): gated behind Slice W + the Stage 0 scrub (Slice 19); sequencing comment posted 2026-06-10. Do not start the spec document before Slice W merges.
- **#541** (Open Referral engagement): soft-gated on the P2 acceptance demo; FA-feed premise scrubbed from its body 2026-06-10.
- **#539** (upstream HSDS PRs), **#558** (interop ladder — Slices 16/17 produce its two-node evidence), **#565/#566/#560/#559** (deferred audits/packages) — touch only when a slice produces something for them.

**Correction protocol (learned from the FA-feed void):** when a premise dies, scrub it the SAME DAY from every public surface that asserts it as live fact — issue bodies (banner + in-place correction), the epic doc, plan docs — and record the correction in the research/decision doc. A public repo's stale issue text is a public claim.

## Critique fixes folded in

`§117`→§6.5/§6.6 (the verify order lives in §6.6 for pull); name the `RefAdapter` dependency in Slices 6/16; the pull chain has NO RFC-9421 transport-signature step (that's the P3 inbox); `fetch.py` DNS-pin + byte-cap land **in Slice 6**; Slice 7 split into 7a/7b; Slice 12 caveat field is **additive-only** (Beacon forward-compat); the live-demo schema isolation is **built** (TEST_DB_SCHEMA is dead config), node-B is **extra services in one project**. Note: `record_version` logs each raw submission, so for multi-origin federated merges it diverges from the merged canonical row (audit-of-submission vs merged-state) — documented, not a regression.
